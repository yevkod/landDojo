import { Box } from "@mui/material";
import React from "react";

const Blog = () => {
  return (
    <Box sx={{ width: "100%", fontSize: "30px", textAlign: "center" }}>
      About
    </Box>
  );
};

export default Blog;
